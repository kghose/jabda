Put these two files under /Library/StartupItems/Diary/

The StartupItems specification requires that the executable file has the 
same name as the subdirectory that contains it and that the executable is in 
the root level of subdirectory
